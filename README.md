# Ivan — update channel

This repo hosts the update manifest (`version.json`) and APK releases for the
Ivan JARVIS assistant app. The app checks `version.json` and installs the APK
attached to the matching release.
